# content_hub 示例配置

| 文件 | 用途 |
|------|------|
| `sources/laughoverlife.example.yaml` | 示例订阅，指向仓库内 `examples/channel_lists/` |

用法：

```powershell
content-hub discover --config examples/content_hub/sources/laughoverlife.example.yaml
```

复制并修改 `content_hub/config/sources.yaml` 后可作为生产配置。
